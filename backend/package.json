{
  "name": "studio-hr-backend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "py -m uvicorn app.main:app --reload --host 0.0.0.0 --port 3001",
    "start": "python -m uvicorn app.main:app --host 0.0.0.0 --port 3001"
  }
}
