@baseUrl = http://localhost:3001

### 健康检查
GET {{baseUrl}}/api/health

### 1) 创建岗位
POST {{baseUrl}}/api/jobs
Content-Type: application/json

{
  "title": "摄影师",
  "mustSkills": ["人像布光", "客户沟通", "拍摄流程控制"],
  "niceSkills": ["短视频", "直播"],
  "responsibilities": "独立完成客片拍摄并把控成片质量"
}

### 2) 生成题库（把 1 替换为你的 jobId）
POST {{baseUrl}}/api/jobs/1/questions/generate

### 3) 创建面试会话
POST {{baseUrl}}/api/interviews
Content-Type: application/json

{
  "jobId": 1,
  "candidateName": "张三",
  "interviewerName": "HR李"
}

### 4) 追加转写（把 1 替换为你的 interviewId）
POST {{baseUrl}}/api/interviews/1/transcripts
Content-Type: application/json

{
  "speaker": "candidate",
  "content": "我在上一家公司主要负责影楼客片拍摄，日均服务5组客户，重点是控制流程和出片质量。"
}

### 5) 生成评估
POST {{baseUrl}}/api/interviews/1/evaluate

### 6) 获取报告
GET {{baseUrl}}/api/interviews/1/report
